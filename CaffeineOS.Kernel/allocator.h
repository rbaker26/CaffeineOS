// I need to define the standered allocators for the c++ stdlib so I can use c++ containers.


namespace std
{
	template<typename T>
	class allocator
	{
		allocator<T>();
		allocator<char>()
		{

		}
	};


}
